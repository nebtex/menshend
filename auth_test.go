package kuper
/*
import (
    "testing"
    . "github.com/smartystreets/goconvey/convey"
    
    "os"
)

func configureVault() {
    
}

func Test_GetConsulAclToken(t *testing.T) {
    os.Setenv("VAULT_ADDR", "http://127.0.0.1:8200")
    KVStoreRoles = []string{
        "consul/creds/admin",
        "consul/creds/devops",
        "consul/creds/fronted"}
    
    Convey("Should return the apropiate consul acl token", t, func() {
        token, leaseDuration, err := GetConsulAclToken("myroot")
        So(err, ShouldBeNil)
        println(token, leaseDuration)
    })
}
*/
